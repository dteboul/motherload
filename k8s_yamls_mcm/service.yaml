apiVersion: app.ibm.com/v1alpha1
kind: Deployable
metadata:
  annotations:
    app.ibm.com/is-local-deployable: "false"
  name: labdt-tradr-service
spec:
  template:
    apiVersion: v1
    kind: Service
    metadata:
      name: labdt-tradr-service
    spec:
      selector:
        app: labdt-tradr
      ports:
        - protocol: TCP
          port: 80
          targetPort: 3000
